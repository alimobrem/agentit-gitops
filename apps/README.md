# apps/

Fleet application manifests watched by the ApplicationSet.

- Real onboarded apps live here (e.g. `pinky/`).
- **Do not** add `apps/agentit/` — AgentIT is self-managed via AgentIT.git (AppSet excluded).
