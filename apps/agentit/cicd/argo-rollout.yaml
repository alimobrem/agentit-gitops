apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  name: agentit
  labels:
    app.kubernetes.io/name: agentit
spec:
  replicas: 2
  selector:
    matchLabels:
      app: agentit
  strategy:
    canary:
      steps:
      - setWeight: 5
      - pause:
          duration: 60s
      - setWeight: 25
      - pause:
          duration: 60s
      - setWeight: 50
      - pause:
          duration: 60s
      - setWeight: 100
      canaryService: agentit-canary
      stableService: agentit
  template:
    metadata:
      labels:
        app: agentit
    spec:
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      containers:
      - name: agentit
        image: image-registry.openshift-image-registry.svc:5000/agentit/agentit:latest
        ports:
        - containerPort: 8080
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: 500m
            memory: 512Mi
        securityContext:
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: true
          capabilities:
            drop:
            - ALL
        livenessProbe:
          httpGet:
            path: /
            port: 8080
          initialDelaySeconds: 10
        readinessProbe:
          httpGet:
            path: /
            port: 8080
          initialDelaySeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: agentit
  labels:
    app.kubernetes.io/name: agentit
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
  selector:
    app: agentit
---
apiVersion: v1
kind: Service
metadata:
  name: agentit-canary
  labels:
    app.kubernetes.io/name: agentit
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
  selector:
    app: agentit
