apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: hello-world
  labels:
    app.kubernetes.io/name: hello-world
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: hello-world